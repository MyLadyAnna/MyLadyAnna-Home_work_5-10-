1. Добавила проверку, если после команды дуэль вводятся буквы или меньшее кол-во чисел чем в айди.
(if message.text.split()[1].isdigit() and len(message.text) == 15:    # 15, тк 9 для айди, 1 пробел и сама команда имеет 5 символов.
...
 else:
        await message.answer('Укажи корректный id! Он состоит из 9 ЦИФР.'))
2. Переделала пару принтов. Например, убрала из вывода выигрыша бота "на столе 0 конфет".
3. В set добавила правило, чтобы кол-во конфет было больше 100 (для того, чтобы игра не заканчивалась за 1-4 хода. Например, юзер может ввести /set 28 и выиграть за 1 ход, если жребий выпадет на него).
(elif count.isdigit() and int(count) < 100:
            await message.answer(f'{message.from_user.first_name}, введи, пожалуйста, число больше 100, чтобы игра была интереснее :)'))
